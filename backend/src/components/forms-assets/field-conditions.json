{
  "collectionName": "forms_assets_field_conditions",
  "info": {
    "displayName": "Condição para aparecer campo",
    "description": ""
  },
  "options": {},
  "attributes": {
    "field": {
      "type": "string",
      "required": true
    },
    "operator": {
      "type": "enumeration",
      "enum": ["equals", "different", "contains", "not-contains"],
      "required": true,
      "default": "equals"
    },
    "value": {
      "type": "string",
      "required": true
    }
  }
}
